<!DOCTYPE html>
<html>
  <head>
    <title>Records list</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <%- include('nav'); %>
		<h3>Records to <%= action %> </h3>
<div id="container" style="width:100%;">     
   	  		<% if (records.length > 0) { %>  
					 <% if (action == 'delete') { %>
<div id="left" style="float:left; width:50%; background-color:Tomato;"> 
<p style="background-color:white; font-size:160%;font-family:verdana ">Select record file to delete: (Cannot be undone)!</p>
</div> 
<div id="right" style="float:right; width:50%; background-color:Tomato;"> 
<p style="background-color:white; font-size:160%;font-family:verdana ">                                                  </p>
</div>
						<% } %>	
						<p>Listing <%= records.length %> records:</p>
<div id="left" style="float:left; width:50%">		
			<% var listKyes = Object.keys(records[0]) ; %>
<label>index</label>	
		  	<% for (var i in listKyes) { %>	  
<label class="listKeys"><%= listKyes[i] %></label>
		  	<% } %>	
		  	<% for (var i in records) { %>
<p><%= i %>: <a href="/<%= action %>/records/<%= records[i].name %>"><%= JSON.stringify(Object.values(records[i])) %></a> </p>
 			<% }  %>     			
 			<% } else { %>
 			Nothing found, check console log for more details. 
 			<% }  %>
</div>  
</div>  
  </body>
</html>